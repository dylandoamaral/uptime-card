import { defaultConfigurationColor, defaultConfigurationTitle } from '../src/default';

export const defaultConfiguration = {
  color: defaultConfigurationColor,
  modules: {
    title: defaultConfigurationTitle,
  },
};
